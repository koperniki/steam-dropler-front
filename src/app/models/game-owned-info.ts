export interface GameOwnedInfo
    {
        gameId: number;

        game: string;

        paymentMethod: string;

        dropConfigId: string;

        dropConfigName: string;

        dropItems: number;

        licenseInfo: string;
    }